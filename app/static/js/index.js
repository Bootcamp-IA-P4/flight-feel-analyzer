// app/static/js/index.js
console.log("Index JS loaded.");

// Aquí puedes añadir validaciones de formulario del lado del cliente
// o cualquier otra interactividad que necesites en el futuro.

// Ejemplo: Validar que la edad no sea negativa al salir del campo
/*
document.addEventListener('DOMContentLoaded', function() {
    const ageInput = document.getElementById('Age');
    if (ageInput) {
        ageInput.addEventListener('blur', function() {
            if (parseInt(this.value) < 0) {
                alert('La edad no puede ser negativa.');
                this.value = ''; // O poner 0
            }
        });
    }
});
*/